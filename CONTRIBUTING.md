# 价格更新规范

本仓库维护可复核的 AI 服务公开价格，不收录无法确认来源的截图价、媒体转述或模型记忆。

## 新增或修改价格

1. 优先使用厂商官方价格页、官方文档或官方价格 API。
2. 同时记录原始单位、币种、区域、套餐门槛、有效期和核验日期。
3. 再按该分类声明的固定工作量生成规范化价格；不得只保留换算结果。
4. 没有稳定公开数字时使用 `quote_required` 或 `dynamic_console`，不得猜测。
5. 免费额度与长期免费价格分开；优惠活动不得覆盖常规目录价。
6. 价格、效果、热度分别记录。没有统一实验时不得发布效果排名。

## 状态值

| 状态 | 含义 |
|---|---|
| `public` | 官方页面有稳定公开数字 |
| `public_tiered` | 官方公开阶梯价 |
| `public_region_specific` | 官方公开价但受区域影响 |
| `dynamic_console` | 需在价格计算器或登录控制台核价 |
| `quote_required` | 需商务出具报价 |

## 复核周期

- API 可直接读取的目录价：每日更新。
- 静态公开价格页：每 30 天复核。
- 询价或控制台动态价：每 90 天检查售卖方式；收到正式报价单时单独记录有效期。

提交 PR 前运行：

```bash
node scripts/validate-catalog.mjs
```
