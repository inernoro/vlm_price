# Security Policy

## 凭据处理

本项目的复现实验需要 OpenRouter API key，但仓库不应包含任何真实密钥。

- 仅通过环境变量或密钥管理服务注入 `OPENROUTER_API_KEY`。
- 不要把 Authorization header、完整请求日志或账户身份响应提交到 Git。
- 若密钥曾出现在聊天、终端日志、Issue、PR 或 Git 历史中，应立即在供应商控制台撤销并重新生成。
- 示例配置必须使用 `<your-key>` 等占位符。

## 报告问题

请通过 GitHub Security Advisory 私下报告可能导致凭据泄露或未经授权访问的问题。不要在公开 Issue 中粘贴密钥、令牌或个人账户数据。

## 数据隐私

上传到第三方 VLM 的图片可能包含敏感信息。生产环境应根据业务要求检查供应商的数据保留、训练使用、区域路由和零数据保留设置，并在上传前删除不必要的个人信息。
